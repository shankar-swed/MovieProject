@model CodWizardsMovieShop.Models.ViewModels.AdminOrderViewModel

@{
	ViewData["Title"] = "DeleteOrder";
	Layout = "~/Views/Shared/_AdminLayout.cshtml";

}

<@* h1>DeleteOrder</h1>
	<div class="alert alert-warning mt-3" role="alert">
	<strong>Warning:</strong> This action cannot be undone.
	</div>
	<div>
	<hr />
	<dl class="row">
	<dt class = "col-sm-2">
	@Html.DisplayNameFor(model => model.OrderDate)
	</dt>
	<dd class = "col-sm-10">
	@Html.DisplayFor(model => model.OrderDate)
	</dd>
	<dt class = "col-sm-2">
	@Html.DisplayNameFor(model => model.Customer.FristName)
	</dt>
	<dd class = "col-sm-10">
	@Html.DisplayFor(model => model.Customer.Email)
	</dd>
	</dl>
	containing following movies...
	@foreach(var item in Model.OrderRows){
	@item.Movie.Title
	}
	<form asp-action="DeleteOrderConfirmed" method="post">
	<input type="hidden" asp-for="Id" />
	<input type="submit" value="delete order" class="btn btn-danger" /> |
	<a asp-action="DisplayOrdersAdmin">Cancel</a>
	</form>
	</div>
	*@

 <div class="d-flex justify-content-center align-items-center mt-5 mb-5">
	<div class="card border-3 border-danger h-100" style="border-radius:7px; width: 60rem;">
		<div class="card-header bg-dark text-white text-center">
			<h3 class="card-title">Delete Order</h3>
		</div>
		<div class="card-body text-center text-dark h-100 m-auto">
			<h5>Are you sure you want to Delete this Order?</h5>
			<div class="mt-3">			
				<partial name="_OrderCustomerPartial" model="Model" />				
			</div>
			<div class="alert alert-warning mt-3" role="alert">
				<strong>Warning:</strong> This action cannot be undone.
			</div>

@* 									<button onclick="plusval1('@Model.Movie.Id','movieNum_@Model.Movie.Id','total_@Model.Movie.Id') " style="color:red">+</button>
 *@
			
				<button onclick="cancelOrder(@Model.OrderId)" class="btn btn-danger mr-2">Delete</button>
				<a asp-action="DisplayOrdersAdmin" asp-controller="Order" class="btn btn-secondary">Cancel</a>
			<
		</div>
	</div>
</div>